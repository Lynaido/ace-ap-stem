{
  "name": "aas-monorepo",
  "version": "1.0.0",
  "description": "Ace AP STEM - Full Stack Application",
  "private": true,
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "dev": "npm run build && npx npm-run-all --parallel dev:frontend dev:backend",
    "dev:frontend": "npm --prefix frontend start",
    "dev:backend": "npm --prefix backend run dev",
    "start": "npx npm-run-all --parallel start:frontend start:backend",
    "start:frontend": "npm --prefix frontend run start:prod",
    "start:backend": "npm --prefix backend run start:prod",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install",
    "clean": "rm -rf node_modules frontend/node_modules backend/node_modules"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
